## Summary

<!-- What changed and why (1–3 sentences). -->

## Checklist

- [ ] `uv run ruff check src tests` and `uv run pytest` pass (or **N/A** explained below)
- [ ] `web_sota`: `npm ci` + `npm run build` if UI or proxy paths changed (or **N/A**)
- [ ] User-facing behavior or env vars changed → **docs** updated (`docs/`, root `README.md`, or `.env.example`)
- [ ] Non-trivial change mostly from an LLM → **noted below** (helps review; optional for tiny fixes)

## N/A / notes

<!-- e.g. “docs-only”, “draft — tests pending”, link to issue -->
